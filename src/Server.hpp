#ifndef multisim_Server_hpp
#define multisim_Server_hpp

namespace multisim{
    class Server{
        public:
            Server();
            ~Server();
            
        protected:

        private:
    };
}

#endif