#ifndef multisim_server_Lobby_hpp
#define multisim_server_Lobby_hpp

namespace multisim{
    namespace server{
        class Lobby{
            public:
                Lobby();
                ~Lobby();
                
            protected:

            private:
        };
    }
}
#endif